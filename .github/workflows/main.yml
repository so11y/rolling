
name: test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  testCommit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '14.x'

      - name: Cahe Node Dependencies
        id: cache
        uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{runner.OS}}-npm-caches-${{ hashFiles('package-lock.json') }}


      - name: add pnpm
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm i pnpm -g

      - name: pnpm i
        if: steps.cache.outputs.cache-hit != 'true'
        run: pnpm i

      - name: test
        run: npx jest & pnpm run coverage
